PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=doc_software
JWT_SECRET=change_this_to_a_random_string_in_production
JWT_EXPIRES_IN=7d
UPLOAD_DIR=uploads
GENERATED_DIR=generated
# Comma-separated list of allowed origins for CORS (production domain goes here)
# Same-origin requests (Nginx proxy) are always allowed regardless of this setting
ALLOWED_ORIGINS=http://localhost:5173
