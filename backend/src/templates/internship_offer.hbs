<!-- internship_offer.hbs  —  compact single-page design -->
<!DOCTYPE html>
<html><head><meta charset="utf-8">
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', Arial, sans-serif; color: #1a1a1a; font-size: 11px; line-height: 1.5; }

  /* ── Top colour bar ── */
  .top-bar { height: 6px; background: linear-gradient(90deg, #7c2d12, #f97316); }

  /* ── Outer double-border frame ── */
  .frame { border: 3px double #c2410c; margin: 5px; padding: 2px; }
  .inner { border: 1px solid #fed7aa; padding: 18px 22px; }

  /* ── Header — float layout ── */
  .header {
    background: #fff7ed; border-bottom: 2px solid #fed7aa;
    padding: 9px 0 10px; margin-bottom: 20px; overflow: hidden;
  }
  .header .logo { float: left; }
  .header .logo img { max-height: 62px; max-width: 180px; display: block; }
  .header .logo .logo-text { font-size: 17px; font-weight: 700; color: #c2410c; }
  .header .company-info { float: right; text-align: right; font-size: 10px; color: #4b5563; }
  .header .company-info .cname { font-size: 14px; font-weight: 800; color: #c2410c; }

  /* ── Title band ── */
  .title-band { background: linear-gradient(90deg, #7c2d12, #f97316); color: #fff; text-align: center; padding: 11px 0 10px; border-radius: 6px; margin-bottom: 20px; }
  .title-band .t-main { font-size: 16px; font-weight: 800; letter-spacing: 2.5px; text-transform: uppercase; }
  .title-band .t-sub  { font-size: 9.5px; letter-spacing: 1.5px; opacity: 0.88; margin-top: 3px; }

  /* ── Ref row — table layout ── */
  table.ref-table {
    width: 100%; border-collapse: collapse;
    margin-bottom: 15px; font-size: 10.5px; color: #6b7280;
  }
  table.ref-table td { border: none; background: none; padding: 0; }
  table.ref-table td:last-child { text-align: right; }

  /* ── Addressee block ── */
  .addr-block { font-size: 10.5px; color: #374151; margin-bottom: 18px; line-height: 1.65; }
  .addr-block .salute { font-size: 12px; font-weight: 700; margin-top: 8px; color: #1a1a1a; }

  /* ── Body text ── */
  .body-text { font-size: 11px; line-height: 1.65; text-align: justify; margin-bottom: 16px; }
  .body-text strong { color: #7c2d12; }
  .body-text p + p { margin-top: 6px; }

  /* ── Details table ── */
  .detail-table { width: 100%; border-collapse: collapse; margin: 10px 0; }
  .detail-table td { padding: 7px 11px; font-size: 10.5px; border: 1px solid #fed7aa; }
  .detail-table tr:nth-child(even) td { background: #fff7ed; }
  .detail-table td:first-child { font-weight: 700; color: #c2410c; width: 38%; background: #ffedd5; }

  /* ── Section divider ── */
  .section-divider { border-top: 1px solid #fed7aa; margin: 23px 0 8px; padding-top: 8px; font-weight: 800; color: #c2410c; font-size: 10.5px; text-transform: uppercase; letter-spacing: 0.5px; }

  /* ── Terms box ── */
  .terms-box { background: #fff7ed; border-left: 4px solid #f97316; border-radius: 5px; padding: 10px 13px; margin: 4px 0; font-size: 10.5px; color: #374151; }
  .terms-box ul { margin: 0; padding-left: 15px; }
  .terms-box li { margin-bottom: 7px; line-height: 1.55; }
  .terms-box li:last-child { margin-bottom: 0; }

  /* ── Signature section — right-aligned only, no seal ── */
  .sig-section { margin-top: 30px; text-align: right; }
  .sig-section img { max-height: 46px; max-width: 130px; display: block; margin-left: auto; margin-bottom: 3px; }
  .sig-line { display: inline-block; border-top: 1.5px solid #374151; padding-top: 4px; margin-top: 4px; min-width: 170px; text-align: center; }
  .sig-name  { font-weight: 700; font-size: 11px; }
  .sig-title { font-size: 10px; color: #6b7280; }

  /* ── Acceptance block ── */
  .accept-block { margin-top: 24px; border: 1.5px dashed #f97316; border-radius: 6px; padding: 16px 15px; font-size: 11px; color: #374151; background: #fff7ed; }
  .accept-title { font-weight: 800; color: #c2410c; font-size: 11px; margin-bottom: 6px; text-transform: uppercase; letter-spacing: 0.5px; }

  /* ── Sign row — generous space for physical signature ── */
  table.sign-table {
    width: 100%; border-collapse: collapse; margin-top: 100px; font-size: 10px; color: #6b7280;
  }
  table.sign-table td {
    border: none; background: none; padding-top: 3px;
    border-top: 1px solid #9ca3af; text-align: center;
  }

  /* ── Footer ── */
  .footer { margin-top: 24px; border-top: 2px solid #fed7aa; padding-top: 6px; text-align: center; font-size: 9px; color: #9ca3af; }
  .footer strong { color: #c2410c; }
</style>
</head>
<body>

<div class="top-bar"></div>

<div class="frame">
<div class="inner">

  <!-- Header: logo left, company info right via float -->
  <div class="header">
    <div class="logo">
      {{#if company.logo_url}}
        <img src="{{company.logo_url}}" alt="{{company.name}}" />
      {{else}}
        <div class="logo-text">{{company.name}}</div>
      {{/if}}
    </div>
    <div class="company-info">
      <div class="cname">{{company.name}}</div>
      <div>{{company.address}}{{#if company.city}}, {{company.city}}{{/if}}{{#if company.state}}, {{company.state}}{{/if}}</div>
      <div>{{company.email}} | {{company.phone}}</div>
      {{#if company.website}}<div>{{company.website}}</div>{{/if}}
    </div>
  </div>

  <!-- Title Band -->
  <div class="title-band">
    <div class="t-main">Internship Offer Letter</div>
    <div class="t-sub">Official Offer for Internship Programme</div>
  </div>

  <!-- Ref Row: ref left, offer date right via table -->
  <table class="ref-table">
    <tr>
      <td>Ref No: <strong>{{#if data.ref_no}}{{data.ref_no}}{{else}}{{doc_number}}{{/if}}</strong></td>
      <td>Date: <strong>{{data.issue_date}}</strong></td>
    </tr>
  </table>

  <!-- Addressee Block -->
  <div class="addr-block">
    <strong>To,</strong><br>
    <strong>{{data.intern_name}}</strong><br>
    {{#if data.designation}}<span>{{data.designation}}</span><br>{{/if}}
    {{#if data.roll_no}}<span>Roll No: {{data.roll_no}}</span><br>{{/if}}
    {{#if data.college}}{{data.college}}{{/if}}
    {{#if data.course}}<br>{{data.course}}{{#if data.branch}} ({{data.branch}}){{/if}}{{/if}}
    {{#if data.address}}<br>{{data.address}}{{/if}}
    {{#if data.mobile_no}}<br>Mobile: {{data.mobile_no}}{{/if}}
    {{#if data.email_id}}<br>{{data.email_id}}{{/if}}
    <div class="salute">Dear {{data.intern_name}},</div>
  </div>

  <!-- Opening Paragraphs -->
  <div class="body-text">
    <p>
      We are pleased to offer you an internship at <strong>{{company.name}}</strong>{{#if data.department}}
      in the <strong>{{data.department}}</strong> department{{/if}}. We were impressed with your profile
      and are confident you will make a valuable contribution to our team during your time with us.
    </p>
    <p>
      Your internship is scheduled to commence on <strong>{{data.from_date}}</strong>.
      Please find the complete details of your internship offer below.
    </p>
  </div>

  <!-- Offer Details Table -->
  <table class="detail-table">
    <tr><td>Intern Name</td><td><strong>{{data.intern_name}}</strong></td></tr>
    {{#if data.designation}}<tr><td>Designation</td><td>{{data.designation}}</td></tr>{{/if}}
    {{#if data.roll_no}}<tr><td>Roll No / Register No</td><td>{{data.roll_no}}</td></tr>{{/if}}
    {{#if data.college}}<tr><td>Institution</td><td>{{data.college}}</td></tr>{{/if}}
    {{#if data.course}}<tr><td>Course / Degree</td><td>{{data.course}}{{#if data.branch}} &mdash; {{data.branch}}{{/if}}</td></tr>{{/if}}
    {{#if data.mobile_no}}<tr><td>Mobile No</td><td>{{data.mobile_no}}</td></tr>{{/if}}
    {{#if data.email_id}}<tr><td>Email ID</td><td>{{data.email_id}}</td></tr>{{/if}}
    {{#if data.pan_no}}<tr><td>PAN No</td><td>{{data.pan_no}}</td></tr>{{/if}}
    <tr><td>Department / Domain</td><td>{{data.department}}</td></tr>
    {{#if data.project_title}}<tr><td>Project / Role</td><td>{{data.project_title}}</td></tr>{{/if}}
    <tr><td>Internship Start Date</td><td><strong>{{data.from_date}}</strong></td></tr>
    {{#if data.stipend}}<tr><td>Monthly Stipend</td><td><strong>&#8377;{{data.stipend}}</strong> per month</td></tr>{{/if}}
    <tr><td>Reporting To / Supervisor</td><td>
      {{#if data.supervisor}}{{data.supervisor}}{{else}}{{#if data.mentor_name}}{{data.mentor_name}}{{else}}&mdash;{{/if}}{{/if}}
    </td></tr>
    <tr><td>Work Location</td><td>{{company.address}}{{#if company.city}}, {{company.city}}{{/if}}{{#if company.state}}, {{company.state}}{{/if}}</td></tr>
    <tr><td>Offer Date</td><td>{{data.issue_date}}</td></tr>
  </table>

  <!-- Terms & Conditions -->
  <div class="section-divider">Terms &amp; Conditions of Internship</div>

  <div class="terms-box">
    <ul>
      <li>This internship is offered for professional training and development purposes and does not constitute, in any manner, an employment contract or relationship with <strong>{{company.name}}</strong>.</li>
      <li>The intern shall devote full attention and effort during designated working hours and maintain professional conduct throughout the internship period.</li>
      <li>All information, data, processes, and business matters of <strong>{{company.name}}</strong> shall be treated as strictly confidential and shall not be disclosed to any third party during or after the internship.</li>
      <li>Any intellectual property, work product, or deliverables created during the internship shall be the exclusive property of <strong>{{company.name}}</strong>.</li>
      <li>The intern is expected to comply with all company policies, code of conduct, and applicable laws and regulations.</li>
      <li>Either party may terminate this internship arrangement by giving reasonable prior written notice to the other party.</li>
      {{#if data.stipend}}<li>The monthly stipend of <strong>&#8377;{{data.stipend}}</strong> is the gross amount and is subject to applicable statutory deductions as per prevailing laws.</li>{{/if}}
      {{#if data.joining_instructions}}<li>{{data.joining_instructions}}</li>{{/if}}
      <li>The intern shall report to the designated supervisor and adhere to the prescribed schedule and work plan for the internship period.</li>
    </ul>
  </div>

  <!-- Closing Paragraph -->
  <div class="body-text">
    <p>
      We look forward to welcoming you to <strong>{{company.name}}</strong> and are confident that this
      internship will be a rewarding and enriching experience. Please confirm your acceptance of this offer
      by signing below. This offer is valid for <strong>7 days</strong> from the date of issue.
    </p>
    {{#if data.remarks}}<p style="font-size:10.5px; color:#6b7280; margin-top:5px;"><em>Note: {{data.remarks}}</em></p>{{/if}}
  </div>

  <!-- Signature Section: right-aligned only, no seal -->
  <div class="sig-section">
    {{#if company.signature_url}}<img src="{{company.signature_url}}" alt="Signature" />{{/if}}
    <div class="sig-line">
      <div class="sig-name">{{company.signatory_name}}</div>
      <div class="sig-title">{{company.signatory_designation}}</div>
      <div class="sig-title">{{company.name}}</div>
    </div>
  </div>

  <!-- Acceptance Block -->
  <div class="accept-block">
    <div class="accept-title">Acceptance by Intern</div>
    <p style="margin: 0; font-size: 10.5px;">
      I, <strong>{{data.intern_name}}</strong>, hereby accept the internship offer extended by
      <strong>{{company.name}}</strong> and agree to abide by all the terms and conditions stated above.
      I confirm that the details mentioned in this offer letter are correct to the best of my knowledge.
    </p>
    <table class="sign-table">
      <tr>
        <td>Intern Signature</td>
        <td>Place</td>
        <td>Date</td>
      </tr>
    </table>
  </div>

  <!-- Footer -->
  <div class="footer">
    <strong>{{company.name}}</strong> &nbsp;|&nbsp; {{company.address}}{{#if company.city}}, {{company.city}}{{/if}}{{#if company.state}}, {{company.state}}{{/if}} &nbsp;|&nbsp;
    {{company.email}} &nbsp;|&nbsp; {{company.phone}}
  </div>

</div>
</div>

</body></html>
