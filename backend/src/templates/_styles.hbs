<style>
  * { box-sizing: border-box; }
  body { font-family: 'Segoe UI', Arial, sans-serif; color: #222; font-size: 12.5px; line-height: 1.55; margin: 0; }
  .page { padding: 0; }
  .header { display: flex; justify-content: space-between; align-items: center; border-bottom: 3px solid #1e40af; padding-bottom: 12px; margin-bottom: 25px; }
  .header .logo img { max-height: 65px; max-width: 200px; }
  .header .company-info { text-align: right; font-size: 11px; color: #444; }
  .header .company-info .cname { font-size: 16px; font-weight: 700; color: #1e40af; }
  h1.title { text-align: center; font-size: 18px; text-decoration: underline; margin: 18px 0 22px; letter-spacing: 1px; }
  .meta { display: flex; justify-content: space-between; margin-bottom: 18px; font-size: 12px; }
  .meta .doc-no { font-weight: 600; }
  p { margin: 8px 0; text-align: justify; }
  .salutation { margin-top: 12px; font-weight: 600; }
  .signature-block { margin-top: 50px; }
  .signature-block .sig-img { max-height: 55px; }
  .signature-block .sig-name { font-weight: 600; margin-top: 4px; }
  .footer { margin-top: 35px; border-top: 1px solid #ccc; padding-top: 8px; text-align: center; font-size: 10px; color: #666; }
  table { width: 100%; border-collapse: collapse; margin: 12px 0; }
  th, td { border: 1px solid #999; padding: 7px 9px; font-size: 12px; }
  th { background: #1e40af; color: #fff; text-align: left; }
  .text-right { text-align: right; }
  .text-center { text-align: center; }
  .total-row { background: #f3f4f6; font-weight: 700; }
  .grand-row { background: #1e40af; color: #fff; font-weight: 700; }
  .badge { display: inline-block; background: #fef3c7; color: #92400e; padding: 2px 8px; border-radius: 4px; font-size: 10px; }
  .info-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 6px 20px; margin: 10px 0 18px; font-size: 12px; }
  .info-grid .label { color: #555; }
  .info-grid .val { font-weight: 600; }
</style>
